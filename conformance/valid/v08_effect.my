// SPDX-License-Identifier: MPL-2.0
// Conformance: use import and lambda expression

use std::io;

fn apply(f: Int -> Int, x: Int) -> Int {
    return f(x);
}

fn main() -> Int {
    let double = |n| => n * 2;
    return apply(double, 5);
}
